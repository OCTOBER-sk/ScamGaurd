Midas, final micro-fix (strings only): the provider paid-note badge on OpenAI/Anthropic cards truncates ("No free tier — requires pa..."). Shorten the badge copy so it always fits on one line without ellipsis.

In src/strings/en.json, change providerPaidNote from "No free tier — requires paid account" to "No free tier" (matches PLAN-FRONTEND.md §3.2 wording "small 'no free tier' note"; the card value-prop note carries the rest of the context).

Touch ONLY src/strings/en.json. Run npm test (254/254) and npm run lint (0). Report the new value.
