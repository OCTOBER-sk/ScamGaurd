Midas, one tiny final fix on ScamGuard options page — copy-level only.

PROBLEM: On the provider cards, OpenAI and Anthropic show the note text "No free tier — requires paid account" (from en.json keys providerOpenaiNote / providerAnthropicNote) AND separately the paid badge (providerPaidNote) with the SAME text. The sentence is duplicated on the card, which looks unpolished.

FIX (strings only, src/strings/en.json):
- Change providerOpenaiNote to: "Powerful reasoning and vision models" (or similar honest, calm value-prop — the "why you would pick this" line, distinct from Gemini's "Best overall quality, built-in vision")
- Change providerAnthropicNote to: "Excellent long-context reasoning" (or similar)
- Keep providerPaidNote unchanged — the badge stays the single "no free tier" signal.
- The text must not overclaim and must stay in the calm/factual tone.

CONSTRAINTS:
- Touch ONLY src/strings/en.json. No JS, no CSS, no HTML.
- npm run lint must stay 0 violations. npm test must stay 254/254.
- No harness regen needed (options-harness links real options.js which imports en.json).

SELF-REVIEW: run git status (only en.json modified), npm test, npm run lint. Report the two new strings and the counts.
