{
  "name": "scamguard",
  "version": "0.1.0",
  "private": true,
  "description": "BYOK scam-risk checker for OLX & Quikr listings + Message & Payment Check.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "lint": "node scripts/lint-strings.js"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "playwright-core": "^1.62.1"
  }
}
